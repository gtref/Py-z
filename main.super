import "test_lib";

fn main() -> int {
    let p: Vec2 = new_vec(1.5, 2.5);

    let i: int = 0;
    while i < 3 {
        print(p.x);
        p.x = p.x + 1.0;
        i = i + 1;
    }

    return 0;
}
